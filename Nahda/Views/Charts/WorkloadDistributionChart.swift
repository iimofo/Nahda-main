import SwiftUI
struct WorkloadDistributionChart: View {
    let tasks: [Task]
    let team: Team
    
    var body: some View {
        VStack {
            // Implement workload distribution visualization
            Text("Workload Distribution")
        }
    }
} 
